# Changelog

本文件记录 ESPaperPlay 的所有显著变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。版本号唯一来源是
`components/board/include/espaperplay_config.h` 中的 `ESPAPERPLAY_VERSION_*`。

## [未发布]

### 变更

- README 精简重构：主 README 仅保留简介 / 功能总览 / 硬件平台 / 文档索引 /
  编译方法 / 路线图；模块级设计与实现细节原样拆分至 `docs/` 下的 10 篇
  双语主题文档（架构、显示、字体、界面与输入、存储、网络时间、天气、
  阅读器、电源与可靠性、Web 管理控制台）。

## [0.2.0] - 2026-09-06

阅读器成型（TXT + EPUB）、完整设备 UI（主界面 / 天气页 / 文件 / 设置 / 首次开机向导）、
自动浅睡眠低功耗体系、GT911 触摸自愈与 SD 诊断日志，并根治 EPD SPI 传输断流。

### 新增

- **TXT 阅读器基础**（阅读器应用的起点）
  - TXT 解析渲染与阅读视图：惰性分页（打开即显示，总页数后台统计）、
    底边栏（目录 / 页码跳转 / 字号 / 灰度刷新）、翻页手势与物理按键；
    大文件采用稀疏检查点跳转，不整书载入。
  - 主界面「阅读器」应用：阅读历史与 SD 卡图书书架，点击续读 / 开新书；
    阅读进度持久化，跨重启恢复。
  - 分页缓存扩展到 TXT：与 EPUB 共用公共 `pagen` 分页模块，缓存键由书
    指纹 + 字号档位 + 内容区尺寸计算，二次打开免重算。
- **EPUB 阅读支持（reader 组件 + 阅读视图重构）**
  - 新增 `espaperplay_reader_epub`：ZIP 容器仅解析中央目录，条目（章节 /
    图片 / 字体）按需 zlib raw inflate 到 PSRAM、用后释放，不整书载入；
    支持 container.xml → OPF（manifest + spine + dc:title）书目解析与
    href 相对路径 / 百分号解码 / 实体解码。
  - XHTML 单遍解析为统一块模型（`espaperplay_reader_blocks`）：标题层级 /
    粗体 / 斜体 / 引用 → 块样式位，`<img>` 与 SVG 内 `<image xlink:href>`
    → 图片块，ruby 注音（rt/rp）与 style/script/head 内容丢弃；章节按需
    驻留（同一时刻仅一章在内存）。
  - 图片渲染：JPEG 走 TJpgD 逐 MCU 流式解码、PNG 走 zlib 流式逐行解码
    （unfilter + 行内抽样，支持 1-16 位深全颜色类型 / 调色板 / tRNS，
    峰值内存 ≈ 目标 RGB565 缓冲 + 2 行扫描线——不做整图 RGBA 解码，大图
    在 LVGL 分配器下必然失败）；图片块独占一页，按 2 倍内容区预算解码后由
    `lv_image` 等比缩放至恰好内接内容区并居中，同一时刻仅缓存一张
    （PNG 解码与 PIL 参考逐像素校验一致）。
  - CSS 版式子集：解析章节样式表的类规则（`text-align` 居中/右对齐、
    `font-weight/style` 粗斜体、`text-indent: 0` 覆盖）与元素规则
    `p { text-indent: 2em }`（CJK 小说正文首行缩进，解析器直接插入全角
    缩进）；块级样式（标题层级、对齐）随标签开合入栈继承给内部段落。
    字体族 / 颜色 / 阴影 / 逐字变号艺术字不还原（BW 墨水屏无色彩，
    内嵌字体多为缺字子集）。
  - 阅读视图（screen_reader）重构为格式无关的块模型分页渲染：文本页按块
    样式选择字体（标题放大加粗、粗/斜体经 FreeType 样式合成），底边栏 /
    页码跳转 / 字号 / 灰度 / 手势对 TXT 与 EPUB 完全复用；全局页码 =
    各章页数前缀和（单章文档（TXT 大文件）仍为惰性分页 + 稀疏检查点跳转，
    打开即时）。
  - 总页数后台统计按时间预算切片（每 tick ≤8ms），跨 tick 分页推进，
    不阻塞翻页；阅读进度改存「章 + 章内页」打包值，恢复进度无需预数
    前缀章节（直接定位目标章）。
  - 分页数据缓存到 SD（/sdcard/system/cache/reader/*.pag，键含书指纹 +
    字号档位 + 内容区宽高，自动失效）：每章分页完成后由专用 worker 异步
    落盘，再次打开时页边界直接载入（首开一章 33s → 二开 0ms），TXT 与
    EPUB 通用（TXT 大文件二开同样免重算），且中途换出再回到视图章同样
    秒级恢复；预取超时改为同步装载兜底，修复 worker 繁忙时章节被误标
    0 页损坏总页数的问题；翻页前确保视图章驻留（计数换出后
    交互不再读到错误分页状态）。
  - 章节解析移出 LVGL 线程：预取 worker（内部 RAM 栈、优先级低于 LVGL）
    负责 ZIP 解压 + CSS + XHTML 块化，产出数据包进就绪槽，翻章时 LVGL
    零解析开销直接采纳；解析结果二进制缓存到 SD（键含 mtime/size 自动
    失效），二次打开章节装载为一次小文件读；后台页数统计经轮询接口
    （poll）消费预取结果，绝不在 LVGL 线程内联解析。
  - 阅读器主页「SD 卡图书」扫描与文件浏览页直接打开均支持 .epub；
    状态栏标题显示书名（EPUB dc:title / TXT 文件名）。
  - 说明：EPUB 内嵌字体仅解析登记不直接用于正文（发布字体多为缺字子集，
    正文以 SD 完整字库优先的系统字体渲染）；ESP32-S3 无 JPEG 硬件解码器
    （属 ESP32-P4 外设），采用内存占用最小的 TJpgD 软解码 + 抽样路径。
- **阅读器目录（TOC）显示与跳转**：底边栏新增「目录」按钮，打开全屏目录
  面板（按屏幕高度分页，◀ ▶ 翻页，自动定位到当前章所在页），点击条目
  跳转对应章首；章名即目录标题（toc.ncx / nav），无标题显示“第 N 章”。
  打开目录时页面手势让位，单击 BOOT 键或点击面板外关闭。
- **阅读器插图页自动灰度刷新**：插图页渲染完成后自动触发一次 GRAY4 四灰阶
  全屏刷新（插图为 RGB565 抖动到 BW 的场景下，灰阶显示明显更细腻）；页面
  切换时照常恢复黑白模式。可在设备端设置页「设备 → 阅读器 → 插图灰度刷新」
  或 Web 管理页「屏幕设置」中开关（默认开，NVS 持久化，即时生效）。
- **文件管理**
  - 设备端文件管理页（主界面「文件」应用）：浏览 SD 卡目录，点击打开
    文本 / EPUB 交由阅读器，图片查看等；空卡 / 拔卡不崩溃。
  - Web 管理页 SD 卡文件管理：上传 / 下载 / 新建目录 / 重命名 / 删除
    （`/api/files*`），手机电脑直接管理卡内书籍与字体。
- **首次开机引导向导 + 工厂重置**：首次开机（未配置）进入引导，支持设备端
  本机配置与 WebUI 扫码两种方式完成网络 / 天气等初始配置（`/api/setup/*`）；
  Web 管理页提供恢复出厂设置。
- **WiFi 扫描：附近网络列表选择**（设备端 + Web 端）
  - wifi 服务新增阻塞式扫描 API（`espaperplay_wifi_scan_start` /
    `espaperplay_wifi_scan_get_results`）：结果按信号强度降序、按 SSID
    去重缓存（隐藏网络不返回）；纯 AP 热点模式扫描时临时切换 APSTA
    （软 AP 保持运行、已连接站点不掉线），完成后自动恢复。
  - 设置页新增「WiFi 网络」行，打开独立列表页（screen_wifi_list）：信号
    图标 + 加密标记，当前已连网络反色高亮；点选加密网络弹出密码键盘
    （开放网络免密直连），连接中独立视图显示进度、可取消；取消 / 超时且
    原模式为 AP 时自动恢复热点，保证设备仍可经 WebUI 访问。
  - 首次开机引导「本机配置」改为扫描列表选择 + 「手动输入」兜底（隐藏
    网络），加密网络空密码校验拦截，键盘取消回到列表不再跳回欢迎页。
  - Web 管理页「网络设置」与开机向导网络步骤新增「扫描附近网络」按钮
    与结果列表（`GET /api/wifi/scan`，点选自动填入 STA SSID 并切换 STA
    模式）。
- **设备端设置页（主界面「设置」应用入口）**
  - 支持不同分辨率（行高/间距按屏高缩放，基准 800px，含下限保证
    可读性；字号保持固定档位避免 FreeType 缓存被挤爆）。宽度方向本就按
    屏宽自适应。
  - 动态分页：按可用高度自动决定每页放几个大类（空间够则同页多类，如
    竖屏 480x800 下「设备 + 系统」同页；横屏 800x480 下自动拆为 3 页），
    大类名称以横线分隔符形式呈现（居中文字 + 两侧横线），子类之间用
    黑底白字条明显分界（墨水屏仅黑白双色，阈值判定下浅灰=白不可见）；
    左右滑动切换分页，底部指示点；墨水屏不滚动。
  - 整行点击触发（滑动优先于点击，互不冲突）：数值步进模态（[-] [+] +
    完成）编辑屏幕空闲自动睡眠、连续局刷强制全刷阈值、BOOT 键长按判定时间，
    修改立即持久化到 NVS 并同步应用到运行中的服务，无需重启。
  - BOOT 键长按动作点击循环切换（全屏刷新 / 返回上一页 / 无操作），立即生效；
    WiFi 模式点击循环切换（热点 / 站点），切换前弹二级确认（网络会重新
    连接、IP 可能变化），确认后自动重新应用网络。
  - 难以输入的配置项（WiFi 凭据、天气 API Key / 位置 / API Host）仅展示
    当前状态并提示「请在 Web 管理页配置」（设备无键盘，长文本不适合在
    墨水屏上输入）。
  - 字体选择：列出出厂内置 + SD 卡字体，点击选用（重启后生效）；
    软件版本行展示当前固件版本；开发者组提供测试页入口。
  - 设置行名称与数值垂直居中对齐（同一水平中心线）；
    边缘滑动或物理按键单击返回；模态打开时单击先关闭模态。
- **SD 卡完整字库（优先）+ WebUI 字体管理**
  - 检测到 SD 卡时，字体组件优先加载 `/sdcard/system/fonts/` 目录下的**完整字库**
    （盘符 `B:`，经 POSIX 代理的 LVGL 文件系统盘符），替代 Flash 中的裁剪字形子集，
    字符覆盖更全；未检测到卡或文件缺失时自动回退 Flash 子集，行为不变。
    字体目录置于 SD 根目录的 `system/` 顶层下，与小说 / EPUB 等用户内容物理隔离。
  - **Web 管理页字体管理**：支持在 WebUI 上传字体文件（.ttf / .otf / .ttc）到 SD 卡、
    枚举已上传字体并选择设备当前使用的字体（NVS 持久化）；所选字体不在 SD 卡时
    自动回退出厂内置字体，渲染不会中断。页面同时给出 Noto Sans SC 的下载入口提示。
    字体选择重启设备后生效。
- **自动浅睡眠与低功耗体系**
  - 用户活动追踪：触摸 / 按键 / 刷新即记活动，空闲超时（可配置，设备端
    设置页 + Web 管理页，NVS 持久化）自动进入浅睡眠；Web 控制台打开期间
    心跳（`/api/heartbeat`）抑制自动睡眠，远程操作不被睡眠打断。
  - 睡眠前主动断开 WiFi 并抑制自动重连；分钟对齐的定时器唤醒（整分醒），
    睡眠中也按周期拉取天气，亮屏数据即新。
  - 软件 RTC 漂移标定补偿：利用定时器唤醒测量睡眠时钟漂移并补偿，
    无外部 32k 晶振也能长期走时准确。
  - 统一状态栏：时间 / WiFi / 睡眠（节能）图标同一路径刷新，睡眠唤醒
    状态一目了然。
- **双线程并行启动 + 开机日志屏**：屏幕点亮不再被 SD / 触摸初始化阻塞
 （boot_display 任务先行点亮屏并显示启动进度），与系统初始化并行，
  开机可感时间明显缩短。
- **GT911 触摸失效诊断与自愈**
  - 触摸驱动健康巡检：定期检查触摸控制器应答，检测失效；针对复位后
    INT 采样导致 I2C 地址锁存翻转（0x5D → 0x14）的失效模式，自动
    复位恢复并回读确认，带退避的重试自愈，触摸无响应不再需要断电重启。
  - Web 管理页远程触发触摸诊断（`POST /api/system/touch_diag`），
    远程即可复位 / 检查触摸链路。
- **诊断日志组件 diaglog**：SD 卡诊断日志（`espaperplay_diaglog_write`，
  独立于 stdio/FATFS 调试打印的小栈安全写入）；开机 / 睡眠 / 唤醒等
  生命周期事件落盘，构建失效时间线；周期心跳日志附带内部 RAM 与
  DMA 堆统计，内存问题可回溯。
- **天气页天文与月相**
  - 天文子页日出日落 / 月出月落弧线（太阳 / 月亮图标按当前时间在弧线上
    定位），月亮图标按 API 返回的真实月相（`moonPhase.icon` 800–807 段）
    显示；当天无月出 / 月落（正常天文现象，每月 1–2 天）显示「--」占位并
    自动回填相邻日期数据，保证显示完整。
  - 天气页新增「最近更新」动态标签；数据过期时自动催促后台立即刷新。
- **Web 管理页改版**：控制台改为侧边栏多级菜单布局，功能分区（网络 /
  屏幕 / 天气 / 字体 / 文件 / 系统）随功能增长保持清晰。
- **CI / CD**：GitHub Actions 构建上传完整烧录镜像（bootloader / 分区表 /
  APP / fonts 分区 / flash_args）；打 `v*` tag 自动创建 GitHub Release，
  打包完整固件 tar.gz（附 FLASH.txt 烧录说明与 SHA256SUMS），Release
  正文自动提取自 CHANGELOG 对应版本段。

### 变更

- **ESP-IDF 升级至 6.1.0**（含依赖锁文件更新），CI 构建容器同步升级。
- 连续局刷后强制全刷的默认阈值由 10 次调整为 20 次（残影清理频率减半，
  仍可在设置页 / Web 管理页调整）。
- 边缘滑动（翻页 / 返回）触发宽度调整为 24 像素，降低误触。
- 触控链路重构：去除中间诊断任务与日志，改为 LVGL 线程直读双队列
 （按键 / 触摸隔离），修复屏幕键盘输入时触摸丢点击的问题。

### 修复

- **EPD SPI 传输断流（根治）**：PSRAM 帧数据经 SPI 传输时经历三个阶段——
  逐事务 4KB 内部 bounce 缓冲会触发 `ESP_ERR_NO_MEM`（内部堆碎片）；
  `SPI_TRANS_DMA_USE_PSRAM` 直读又因 PSRAM 被 LVGL / FreeType 争抢、
  EDMA 建立延迟不足导致 DMA TX underflow。最终改为初始化时预留**持久
  内部 RAM DMA 暂存区**整帧搬运，彻底根治；刷新 worker 对偶发失败做
  有界重试（2 次，200/400ms 退避）兜底。
- **阅读器（自 v0.1.0 后的修复）**
  - EPUB 历史进度显示语义错乱：主页「第 9 章 4/480 页」页号是章内页而
    总数是全书页数，不可比。保存进度时把当前章的章内总页数一并写入
    （total 字段打包，bit31 标记新格式），显示自洽的「第 9 章 4/49 页」；
    旧格式记录降级显示；TXT 不受影响。
  - 章节切分以目录为准：此前把 spine 中全部 XHTML 文档（封面 / 扉页 /
    插图页 / 后记等）每个当作一章；现按 toc.ncx（EPUB2）/ nav 文档
    （EPUB3）切分，目录项标题即章名；目录外的独立插图文档并入相邻章节
    （内容不丢）；无目录退化为 spine 即章。章节包与分页缓存版本升级，
    旧缓存自动失效重建。
  - 分页缓存归属错误：后台计数换出驻留章后，分页表初始化仍按上一次章节
    归属读取 SD 缓存，把别的章的页边界 / 页数套到当前章（典型表现：从
    某章第 1 页按「上一章」落到前一章的「第 5 页」，总页数错乱）。现
    `pstarts_init` 显式接收章节号并在查缓存前设定归属；并加固——只有
    分页真正推进到章末才落盘缓存，缓存页数超容量时扩表重读。
  - 目录长标题显示异常：CJK 长串依赖 LVGL 点号模式截断渲染错乱；改为
    构建文本时按字号预算做 UTF-8 边界单行截断（加省略号）。
  - 目录首次打开不显示条目：行宽取自刚创建面板的运行时坐标（布局刷新前
    恒为 0）；改为打开时按屏幕尺寸算术求得行宽。
  - 页码跳总落在末页：目标章为最后一章时被无条件钳到末页；删除多余钳制。
  - 大文件加载慢、总页数任务失败与模态白底覆盖。
- **布局适配**：多分辨率（矮屏 / 横屏）下多页面越屏裁切重做，全部页面
  按屏高缩放布局；此前天气页子页 2/3 低分辨率溢出、主页时钟占位错位、
  设置页测试页归类错误一并修复。
- **天文数据健壮性**：月出回填改用昨日缓存，修复和风 API 对当日参数
  返回 400 导致的解析失败与栈垃圾上屏。
- **睡眠 / 唤醒竞态**：定时器唤醒窗口内触摸恢复后不再立刻又睡；唤醒后
  睡眠图标滞留不消失；唤醒后初次局刷改用真实旧帧，避免强制翻转伤屏。
- **启动稳定性**：修复 LVGL FreeRTOS 启动超时崩溃；修复拔卡后启动崩溃
  并加固文件管理页对异常卡状态的处理。
- **网络时间**：网络恢复后及时重试对时，修复启动时断网导致的永久性
  时间不同步。
- **输入**：修复文件管理页屏幕键盘显示异常（同时禁用光标闪烁，墨水屏
  局刷无收益且留残影）。
- **字体**：修复出厂字体子集缺失位码 0xFE 段汉字（如「件」）显示空白。
- **编译**：reader 自持 TJpgDec vendor 副本补入 `src/libs/tjpgd`（剥离
  LVGL 注入路径，避免被 `LV_USE_TJPGD` 裁空），修复 EPUB 图片解码编译
  错误（仅影响二次开发）。

### 性能

- **LVGL 内存后端整体迁 PSRAM**（`LV_USE_CUSTOM_MALLOC` 自定义
  `lv_malloc_core`）：LVGL 对象 / 渲染缓冲不再挤占内部 RAM。
- **天气服务 52KB 静态缓存迁 PSRAM 惰性分配**（`weather_block_ensure`
  范式），实测释放内部 DRAM 约 51.5KB。
- FreeType 字形缓存 384 → 1024，CJK 长文（阅读器整页中文）不再反复
  从 SD 卡重读字形。

### 移除

- **TOUCH / EPD / SD 电源轨（MOS 管关断）支持**：板上后续不再用 MOS 管控制
  这三个部件的供电，固件按常供电处理。删除 `espaperplay_config.h` 中的
  `ESPAPERPLAY_PIN_EPD_PWR` / `ESPAPERPLAY_PIN_TOUCH_PWR` /
  `ESPAPERPLAY_PIN_SD_PWR` 引脚宏；EPD 驱动的上电函数与
  `espaperplay_epd_power_off()` API、touch 初始化的电源轨上电步骤、SD 驱动
  的 `sd_power_rail_enable/disable()` 及编译开关
  `ESPAPERPLAY_EPD/SD_ENABLE_POWER_PIN` 一并移除；power 服务删除
  `espaperplay_power_domain_t` 电源域枚举与 `espaperplay_power_domain_set()`
  桩（均无调用者）。睡眠/唤醒流程不依赖电源轨，行为不变。

## [0.1.0] - 2026-08-20

首个公开版本：ESP32-S3 电子纸设备平台（7.5" 800×480 EPD，16MB flash / 8MB PSRAM）。

### 新增

- **电子纸（EPD）驱动**（GDEY075T7-T01 / UC8179）
  - 全屏 / 局部差分（N2OCP）/ 快刷 / 4 灰阶刷新模式
  - 强制全像素翻转全刷（FULL_FORCE）清除残影；初始化后清白全刷建立基线
  - 空闲自动深度睡眠（默认 90s，可配置），唤醒自动重新初始化
  - 深度睡眠唤醒首刷全屏基线，消除偶发残影 / 刷新不完整
- **GUI 渲染后端**（RGB565 主帧 + 模式化转换）
  - BW：阈值 / Bayer 抖动；GRAY4：Floyd–Steinberg / Bayer
  - 异步刷新 worker：渲染线程零阻塞、双槽位排队与脏区合并
  - 连续局刷计数强制全刷清残影（无论区域大小，阈值可配置，默认 10）
- **LVGL 集成**
  - FreeType 矢量字体渲染（fonts 分区 + Noto Sans SC 中文子集）
  - 主界面安卓风格桌面（状态栏 + 时钟/应用双页 + 滑动切换 + Iconify 图标）
  - 页面栈导航、百分比布局（适配不同分辨率/横竖屏）、双击旋转屏幕
- **输入**
  - BOOT 物理按键（espressif/button）：短按 / 长按全局默认动作可配置（全刷 / 返回 / 无操作）
  - GT911 电容触摸（I2C，复位地址锁存、INT 中断读取）
- **MicroSD 卡存储**（SDIO/SDMMC 接口）
  - SDMMC 主机 / 槽位 / 卡片底层驱动：CLK=14、CMD=15、D0–D3=16-18/21，PWR 默认关闭
  - 引脚避让 GPIO19/20（芯片内置 USB D-/D+ 焊盘，避免与板载 USB/串口冲突导致断连重启）
  - 卡片探测 / 信息 / 底层扇区读写 API，含可选只读自检
  - FATFS 挂载 + VFS 注册（LFN 长文件名、UTF-8 编码，中文文件名与内容不乱码）
  - SD 卡缺失或未格式化不阻断启动：网络 / 天气 / Web / UI 照常，仅文件类功能（阅读器）不可用
- **网络与时间**
  - WiFi AP / STA 模式（WPA2/WPA3）
  - 和风天气（QWeather）全部 API + 天气页（实时条件 / 24h 气温曲线 / 图标集）
  - netip → geoip → 时区 → NTP 自动对时（带 TTL 内存缓存）
- **Web 管理**
  - HTTPS（运行时生成自签名证书，私钥不出设备）
  - 鉴权（PBKDF2 加盐）与会话管理，登录失败限速锁定
  - 首次使用免密码引导设置管理密码
  - 可配置：WiFi、屏幕空闲超时、全刷阈值、BOOT 长按动作、天气
- **板级 / 构建**
  - 自定义分区表（APP 4MB、fonts 8MB SPIFFS 只读资产）
  - -O2 编译优化 + CPU 动态调频（DFS 240/80MHz）
  - 版本号单一来源（config.h 数字宏），CMake `PROJECT_VER` 同步

### 修复

- EPD 局部刷新条纹伪影（N2OCP 差分架构替代影子缓冲）
- 灰阶切回黑白时的中间灰残留
- 深度睡眠唤醒后首次局刷残影 / 刷新不完整
- 局刷图像错位（GUI 槽位竞态 + 驱动快照）
- GT911 复位时序，开机数分钟无响应
- 天气服务偶发连接失败（LWIP 连接池耗尽）、响应缓冲内存碎片化
- Web 保存 WiFi 配置时先响应再重启
- 空闲自动睡眠定时器重新武装失效
- 屏幕旋转后控件命中错位

### 性能

- 快刷改用 OTP 波形 + PLL 100Hz（实测约 2.4s → 1.1s）
- 局刷 PTOUT 时序调整（实测提速 0~13%）
- TLS 收发缓冲回落 PSRAM + 动态缓冲，防止内部堆碎片化
- LWIP 连接资源池容量调优

### 已知限制

- 暂无 OTA（串口烧录，分区为 factory 无 A/B）
- 首次使用需设置 Web 管理密码（出厂免密码仅限首次设置阶段）
- E-ink 刷新固有闪烁；全刷阈值 / 睡眠时长可在 Web 管理页按需调整

[0.2.0]: https://github.com/xiaobaoaaaaaa/ESPaperPlay/releases/tag/v0.2.0
[0.1.0]: https://github.com/xiaobaoaaaaaa/ESPaperPlay/releases/tag/v0.1.0
