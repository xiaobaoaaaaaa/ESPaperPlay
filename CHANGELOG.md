# Changelog

本文件记录 ESPaperPlay 的所有显著变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。版本号唯一来源是
`components/board/include/espaperplay_config.h` 中的 `ESPAPERPLAY_VERSION_*`。

## [未发布]

### 新增

- **阅读器目录（TOC）显示与跳转**：底边栏新增「目录」按钮，打开全屏目录
  面板（按屏幕高度分页，◀ ▶ 翻页，自动定位到当前章所在页），点击条目
  跳转对应章首；章名即目录标题（toc.ncx / nav），无标题显示“第 N 章”。
  打开目录时页面手势让位，单击 BOOT 键或点击面板外关闭。
- **阅读器插图页自动灰度刷新**：插图页渲染完成后自动触发一次 GRAY4 四灰阶
  全屏刷新（插图为 RGB565 抖动到 BW 的场景下，灰阶显示明显更细腻）；页面
  切换时照常恢复黑白模式。可在设备端设置页「设备 → 阅读器 → 插图灰度刷新」
  或 Web 管理页「屏幕设置」中开关（默认开，NVS 持久化，即时生效）。

- **EPUB 阅读支持（reader 组件 + 阅读视图重构）**
  - 新增 `espaperplay_reader_epub`：ZIP 容器仅解析中央目录，条目（章节 /
    图片 / 字体）按需 zlib raw inflate 到 PSRAM、用后释放，不整书载入；
    支持 container.xml → OPF（manifest + spine + dc:title）书目解析与
    href 相对路径 / 百分号解码 / 实体解码。
  - XHTML 单遍解析为统一块模型（`espaperplay_reader_blocks`）：标题层级 /
    粗体 / 斜体 / 引用 → 块样式位，`<img>` 与 SVG 内 `<image xlink:href>`
    → 图片块，ruby 注音（rt/rp）与 style/script/head 内容丢弃；章节按需
    驻留（同一时刻仅一章在内存）。
  - 图片渲染：JPEG 走 TJpgD 逐 MCU 流式解码、PNG 走 zlib 流式逐行解码
    （unfilter + 行内抽样，支持 1-16 位深全颜色类型 / 调色板 / tRNS，
    峰值内存 ≈ 目标 RGB565 缓冲 + 2 行扫描线——不做整图 RGBA 解码，大图
    在 LVGL 分配器下必然失败）；图片块独占一页，按 2 倍内容区预算解码后由
    `lv_image` 等比缩放至恰好内接内容区并居中，同一时刻仅缓存一张
    （PNG 解码与 PIL 参考逐像素校验一致）。
  - CSS 版式子集：解析章节样式表的类规则（`text-align` 居中/右对齐、
    `font-weight/style` 粗斜体、`text-indent: 0` 覆盖）与元素规则
    `p { text-indent: 2em }`（CJK 小说正文首行缩进，解析器直接插入全角
    缩进）；块级样式（标题层级、对齐）随标签开合入栈继承给内部段落。
    字体族 / 颜色 / 阴影 / 逐字变号艺术字不还原（BW 墨水屏无色彩，
    内嵌字体多为缺字子集）。
  - 阅读视图（screen_reader）重构为格式无关的块模型分页渲染：文本页按块
    样式选择字体（标题放大加粗、粗/斜体经 FreeType 样式合成），底边栏 /
    页码跳转 / 字号 / 灰度 / 手势对 TXT 与 EPUB 完全复用；全局页码 =
    各章页数前缀和（单章文档（TXT 大文件）仍为惰性分页 + 稀疏检查点跳转，
    打开即时）。
  - 总页数后台统计按时间预算切片（每 tick ≤8ms），跨 tick 分页推进，
    不阻塞翻页；阅读进度改存「章 + 章内页」打包值，恢复进度无需预数
    前缀章节（直接定位目标章）；FreeType 字形缓存 256 → 1024，避免 CJK
    章节分页时字形反复从 SD 卡重读。
  - 分页数据缓存到 SD（/sdcard/system/cache/reader/*.pag，键含书指纹 +
    字号档位 + 内容区宽高，自动失效）：每章分页完成后由专用 worker 异步
    落盘，再次打开时页边界直接载入（首开一章 33s → 二开 0ms），TXT 与
    EPUB 通用（TXT 大文件二开同样免重算），且中途换出再回到视图章同样
    秒级恢复；预取超时改为同步装载兜底，修复 worker 繁忙时章节被误标
    0 页损坏总页数的问题；翻页前确保视图章驻留（计数换出后
    交互不再读到错误分页状态）。
  - 章节解析移出 LVGL 线程：预取 worker（内部 RAM 栈、优先级低于 LVGL）
    负责 ZIP 解压 + CSS + XHTML 块化，产出数据包进就绪槽，翻章时 LVGL
    零解析开销直接采纳；解析结果二进制缓存到 SD（键含 mtime/size 自动
    失效），二次打开章节装载为一次小文件读；后台页数统计经轮询接口
    （poll）消费预取结果，绝不在 LVGL 线程内联解析。
  - 阅读器主页「SD 卡图书」扫描与文件浏览页直接打开均支持 .epub；
    状态栏标题显示书名（EPUB dc:title / TXT 文件名）。
  - 说明：EPUB 内嵌字体仅解析登记不直接用于正文（发布字体多为缺字子集，
    正文以 SD 完整字库优先的系统字体渲染）；ESP32-S3 无 JPEG 硬件解码器
    （属 ESP32-P4 外设），采用内存占用最小的 TJpgD 软解码 + 抽样路径。
- **设备端设置页（主界面「设置」应用入口）**
  - 支持不同分辨率（方案 1：行高/间距按屏高缩放，基准 800px，含下限保证
    可读性；字号保持固定档位避免 FreeType 缓存被挤爆）。宽度方向本就按
    屏宽自适应。
  - 动态分页：按可用高度自动决定每页放几个大类（空间够则同页多类，如
    竖屏 480x800 下「设备 + 系统」同页；横屏 800x480 下自动拆为 3 页），
    大类名称以横线分隔符形式呈现（居中文字 + 两侧横线），子类之间用
    黑底白字条明显分界（墨水屏仅黑白双色，阈值判定下浅灰=白不可见）；
    左右滑动切换分页，底部指示点；墨水屏不滚动。
  - 整行点击触发（滑动优先于点击，互不冲突）：数值步进模态（[-] [+] +
    完成）编辑屏幕空闲自动睡眠、连续局刷强制全刷阈值、BOOT 键长按判定时间，
    修改立即持久化到 NVS 并同步应用到运行中的服务，无需重启。
  - BOOT 键长按动作点击循环切换（全屏刷新 / 返回上一页 / 无操作），立即生效；
    WiFi 模式点击循环切换（热点 / 站点），切换前弹二级确认（网络会重新
    连接、IP 可能变化），确认后自动重新应用网络。
  - 难以输入的配置项（WiFi 凭据、天气 API Key / 位置 / API Host）仅展示
    当前状态并提示「请在 Web 管理页配置」（设备无键盘，长文本不适合在
    墨水屏上输入）。
  - 字体选择：列出出厂内置 + SD 卡字体，点击选用（重启后生效）；
    软件版本行展示当前固件版本；开发者组提供测试页入口。
  - 设置行名称与数值垂直居中对齐（同一水平中心线）；
    边缘滑动或物理按键单击返回；模态打开时单击先关闭模态。
- **SD 卡完整字库（优先）**
  - 检测到 SD 卡时，字体组件优先加载 `/sdcard/system/fonts/` 目录下的**完整字库**
    （盘符 `B:`，经 POSIX 代理的 LVGL 文件系统盘符），替代 Flash 中的裁剪字形子集，
    字符覆盖更全；未检测到卡或文件缺失时自动回退 Flash 子集，行为不变。
    字体目录置于 SD 根目录的 `system/` 顶层下，与小说 / EPUB 等用户内容物理隔离。
  - **Web 管理页字体管理**：支持在 WebUI 上传字体文件（.ttf / .otf / .ttc）到 SD 卡、
    枚举已上传字体并选择设备当前使用的字体（NVS 持久化）；所选字体不在 SD 卡时
    自动回退出厂内置字体，渲染不会中断。页面同时给出 Noto Sans SC 的下载入口提示。
    字体选择重启设备后生效。

## [0.1.0] - 2026-08-20

首个公开版本：ESP32-S3 电子纸设备平台（7.5" 800×480 EPD，16MB flash / 8MB PSRAM）。

### 新增

- **电子纸（EPD）驱动**（GDEY075T7-T01 / UC8179）
  - 全屏 / 局部差分（N2OCP）/ 快刷 / 4 灰阶刷新模式
  - 强制全像素翻转全刷（FULL_FORCE）清除残影；初始化后清白全刷建立基线
  - 空闲自动深度睡眠（默认 90s，可配置），唤醒自动重新初始化
  - 深度睡眠唤醒首刷全屏基线，消除偶发残影 / 刷新不完整
- **GUI 渲染后端**（RGB565 主帧 + 模式化转换）
  - BW：阈值 / Bayer 抖动；GRAY4：Floyd–Steinberg / Bayer
  - 异步刷新 worker：渲染线程零阻塞、双槽位排队与脏区合并
  - 连续局刷计数强制全刷清残影（无论区域大小，阈值可配置，默认 10）
- **LVGL 集成**
  - FreeType 矢量字体渲染（fonts 分区 + Noto Sans SC 中文子集）
  - 主界面安卓风格桌面（状态栏 + 时钟/应用双页 + 滑动切换 + Iconify 图标）
  - 页面栈导航、百分比布局（适配不同分辨率/横竖屏）、双击旋转屏幕
- **输入**
  - BOOT 物理按键（espressif/button）：短按 / 长按全局默认动作可配置（全刷 / 返回 / 无操作）
  - GT911 电容触摸（I2C，复位地址锁存、INT 中断读取）
- **MicroSD 卡存储**（SDIO/SDMMC 接口）
  - SDMMC 主机 / 槽位 / 卡片底层驱动：CLK=14、CMD=15、D0–D3=16-18/21，PWR 默认关闭
  - 引脚避让 GPIO19/20（芯片内置 USB D-/D+ 焊盘，避免与板载 USB/串口冲突导致断连重启）
  - 卡片探测 / 信息 / 底层扇区读写 API，含可选只读自检
  - FATFS 挂载 + VFS 注册（LFN 长文件名、UTF-8 编码，中文文件名与内容不乱码）
  - SD 卡缺失或未格式化不阻断启动：网络 / 天气 / Web / UI 照常，仅文件类功能（阅读器）不可用
- **网络与时间**
  - WiFi AP / STA 模式（WPA2/WPA3）
  - 和风天气（QWeather）全部 API + 天气页（实时条件 / 24h 气温曲线 / 图标集）
  - netip → geoip → 时区 → NTP 自动对时（带 TTL 内存缓存）
- **Web 管理**
  - HTTPS（运行时生成自签名证书，私钥不出设备）
  - 鉴权（PBKDF2 加盐）与会话管理，登录失败限速锁定
  - 首次使用免密码引导设置管理密码
  - 可配置：WiFi、屏幕空闲超时、全刷阈值、BOOT 长按动作、天气
- **板级 / 构建**
  - 自定义分区表（APP 4MB、fonts 8MB SPIFFS 只读资产）
  - -O2 编译优化 + CPU 动态调频（DFS 240/80MHz）
  - 版本号单一来源（config.h 数字宏），CMake `PROJECT_VER` 同步

### 修复

- **目录首次打开不显示条目**：行宽取自刚创建面板的运行时坐标（布局刷新前
  恒为 0，与文件浏览器模态同一坑），首屏行尺寸无效不可见；翻页重建时
  布局已完成才正常。改为打开时按屏幕尺寸算术求得行宽。

- **页码跳转落在末页**：跳转解析在目标章为最后一章时无条件钳到该章末页
  （TXT 单章书任何页码都跳到最后一页，EPUB 最后一章同样中招）；删除该
  多余钳制——页码循环本身已给出正确章内页号。

- **章节切分以目录为准**：此前把 spine 中全部 XHTML 文档（封面/扉页/插图
  页/后记等）每个当作一章（样书 31 "章"），与书籍实际章节数（目录 25 项）
  不符。现按 toc.ncx（EPUB2）/ nav 文档（EPUB3）切分章节，目录项标题即章
  名；目录外的独立插图文档并入相邻章节阅读流（内容不丢）；无目录退化为
  spine 即章。章节包缓存与分页缓存版本升级（v2/v3），旧缓存自动失效重建。

- **阅读器分页缓存归属错误**：后台计数换出驻留章后，分页表初始化仍按上一次
  的章节归属读取 SD 缓存，把别的章的页边界/页数套到当前章——典型表现：
  从某章第 1 页按"上一章"却落到前一章的"第 5 页"（该章实际仅 1 页），
  总页数随之错乱。现 `pstarts_init` 显式接收章节号并在查缓存前设定归属
  （上一版此修改因补丁脚本中断未实际落地，本版补齐并逐调用点核对）；
  并加固两点——只有分页真正推进到章末才落盘缓存（中途卡住的残缺表不再
  误存），缓存页数超过当前表容量时扩表重读（大章节缓存不再被误删反复重算）。


- EPD 局部刷新条纹伪影（N2OCP 差分架构替代影子缓冲）
- 灰阶切回黑白时的中间灰残留
- 深度睡眠唤醒后首次局刷残影 / 刷新不完整
- 局刷图像错位（GUI 槽位竞态 + 驱动快照）
- GT911 复位时序，开机数分钟无响应
- 天气服务偶发连接失败（LWIP 连接池耗尽）、响应缓冲内存碎片化
- Web 保存 WiFi 配置时先响应再重启
- 空闲自动睡眠定时器重新武装失效
- 屏幕旋转后控件命中错位

### 性能

- 快刷改用 OTP 波形 + PLL 100Hz（实测约 2.4s → 1.1s）
- 局刷 PTOUT 时序调整（实测提速 0~13%）
- TLS 收发缓冲回落 PSRAM + 动态缓冲，防止内部堆碎片化
- LWIP 连接资源池容量调优

### 已知限制

- 暂无 OTA（串口烧录，分区为 factory 无 A/B）
- 首次使用需设置 Web 管理密码（出厂免密码仅限首次设置阶段）
- E-ink 刷新固有闪烁；全刷阈值 / 睡眠时长可在 Web 管理页按需调整

[0.1.0]: https://github.com/xiaobaoaaaaaa/ESPaperPlay/releases/tag/v0.1.0
